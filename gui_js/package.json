{
  "name": "novel-file-cleanup",
  "version": "1.0.1",
  "description": "소설 파일 중복 검사 프로그램",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win portable",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "build": {
    "appId": "com.novel.cleanup",
    "productName": "Novel File Cleanup GUI 1.0.1",
    "win": {
      "target": "portable",
      "icon": "../icon.ico"
    },
    "directories": {
      "output": "dist"
    },
    "asar": true,
    "files": [
      "src/**/*",
      "package.json"
    ]
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "dependencies": {
    "cli-progress": "^3.12.0",
    "glob": "^7.2.3",
    "iconv-lite": "^0.6.3"
  }
}
